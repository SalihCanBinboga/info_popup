import 'package:info_popup/src/controllers/info_popup_controller.dart';

/// [OnAreaPressed] is called when the tap area is pressed.
typedef OnAreaPressed = void Function(InfoPopupController controller);
